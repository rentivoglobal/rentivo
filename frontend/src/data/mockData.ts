/**
 * Rentivo Mock Data (Decommissioned)
 * All active properties and entities are loaded directly from the Supabase backend.
 */
export { IBADAN_AREAS, PROPERTY_TYPES } from '../types';
export type { MarketplaceListing } from '../types';
export const INITIAL_LISTINGS = [];
